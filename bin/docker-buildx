#!/bin/bash

dir=${1%%/}
imagename=${dir#docker/}
version=${2?Version required}

docker buildx build --no-cache --builder dc1 --platform linux/amd64 --push -t "docker-registry.service.home/falconindy/$imagename:$2" "$1"
